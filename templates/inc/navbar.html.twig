<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href={{ path('home') }}><img src={{ asset('build/images/pangolri.png') }}></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar_main" aria-controls="navbar_main" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbar_main">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href={{ path('home') }}><i class="fas fa-home"></i> Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href={{ path('product_list') }}><i class="fas fa-shopping-bag"></i> Products</a>
            </li>
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href={{ path('show_cart') }}><i class="fas fa-shopping-cart"></i> Cart</a>
            </li>

            <li class="nav-item">
                {% if app.user %}
                    <div class="dropdown">
                        <div data-toggle="dropdown"><a class="nav-link"><i class="fas fa-user"></i> My account</a></div>
                        <div class="dropdown-menu dropdown-menu-right">
                            <a class="text-dark" href="{{ path('show_user') }}"><div class="dropdown-item">Manage my account</div></a>
                            <a class="text-dark" href="{{ path('home') }}"><div class="dropdown-item">My wishlists</div></a> {# @Todo - Changer la redirection #}
                            <div class="dropdown-divider"></div>
                            <a class="text-dark" href="{{ path('app_logout') }}"><div class="dropdown-item">Disconnect</div></a>
                        </div>
                    </div>
                    
                {% else %}
                    <a class="nav-link" href={{ path('app_login') }}><i class="fas fa-sign-in-alt"></i> Login</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>